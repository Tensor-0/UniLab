# 交接：dm10 执行器建模（新会话从这里开始）

> **创建于** 2026-09-17。上一会话上下文约 520K/1M，在此交接。

---

## 一、当前 git 状态（已就绪，无需再动）

```
分支:   dm10          （基于 upstream/main = 84a00d04）
提交:   67e1e141      dm10: 双足人形任务（配置 + 奖励项 + 资产 + 测量脚本）
远端:   origin   → git@github.com:Tensor-0/UniLab.git      你的 fork
        upstream → git@github.com:unilabsim/UniLab.git     上游（会重定向到 Motphys）
快照:   /home/zhan/unilab-dm10-snapshot-20260917-1141.tar.gz  (4.5 MB)
```

**工作区干净**（`git status` 无未提交改动）。
**三层保护**：快照 + 本地分支 + fork 远端。

⚠️ **动手前不要再动 git**。有安全网了，直接改代码即可。

---

## 二、要做什么

**Q2：给 dm10 接上真实的执行器模型（τ-ω），让仿真更接近真机。**

背景：现在 `dm10.xml` 里是 `${10 个 <position> kp=30 kv=3.0, forcerange=±28}$`，
这是个**与速度无关的常数力矩上限** —— 而真机的力矩随转速明显下降。

---

## 三、已完成的准备工作（Q1，全部有实测证据）

### 3.1 达妙 DM-J4340-2EC (24V) 的真实参数

**仓库**：`~/dm-actuator-model/`（GitHub: `Tensor-0/dm-actuator-model`）

| 项 | 值 | 来源 |
|---|---|---|
| **τ-ω 直线** | `ω = −0.118555·τ + 6.401745` rad/s | 官方台架曲线 97 点拟合，**r²=0.9916** |
| 空载转速 | 6.4017 rad/s (61.1 rpm) | 实测外推 |
| **堵转扭矩** | **54 N·m** | ⚠️ **实测选定**，非说明书标称的 40 |
| 连续扭矩 | 12 N·m | 额定 |
| MuJoCo 参数 | `nominal="24 54 6.4017"` | 三个数即可 |

**⚠️ 关键决策记录**：`stall_torque` 用 **54** 不用 40，理由是与官方曲线逐点比对：

| 取值 | 平均误差 | 最大误差 |
|---|---|---|
| **54** | **0.85%** | 3.28% ✅ |
| 40（说明书标称） | 9.94% | 17.00% ❌ |

证据：`~/dm-actuator-model/data/tau_omega_overlay.png`（叠加图）

### 3.2 MuJoCo 原生 `<dcmotor>`（⭐ 已实测可用）

```xml
<dcmotor name="leg_l1_joint_motor" joint="leg_l1_joint"
         nominal="24 54 6.4017"
         input="position"              <!-- ⚠️ MuJoCo 3.11 写法 -->
         controller="30 0 3 0 0 24"    <!-- kp ki kd slewmax Imax Vmax -->
         forcerange="-28 28"/>
```

**已经实测验证**（MuJoCo 3.11.0，UniLab 的 `.venv`）：
- 空载稳态 `ω = V/K`，扫 24/18/12/6/3 V **误差 0.00%**
- `position` 模式跟踪 0.5 rad → 收敛到 **0.4999**
- **τ-ω 生效**：扫 `Vmax` → 峰值力矩线性变化
- 力矩公式数值反推 `τ = (K/R)v − (K²/R)ω`

**验证脚本**：`~/dm-actuator-model/tools/verify_mujoco_dcmotor.py`、
`verify_dcmotor_loaded.py`、`plot_tau_omega.py`

### ⚠️ 3.3 版本陷阱（务必记住）

| MuJoCo 版本 | `input` 写法 |
|---|---|
| **3.11（本机）** | **`input="position"`** / `"velocity"` / `"voltage"` |
| 3.12+ | `input="pos vel"` 等空格分隔签名 |

**本机 3.11 实测 `input="pos vel ff"` → `XML Error: invalid keyword`**。
别照抄网上的 3.12 示例。

---

## 四、⭐ 上游已有完整先例（照抄架构）

**`~/dm-actuator-model/reference/bam_action.py`**（451 行）
来源：`rocPAI-Forge/microduck_rl_unilab`，源自 `Motphys/UniLab` PR #1475

**细读文档**：`~/dm-actuator-model/docs/bam_action_源码细读_20260917.md`

### 核心架构（已对本机框架代码核实）

```python
class XxxAction(ActionTerm):
    requires_substep_state_feedback: ClassVar[bool] = True    # ← 关键声明

    def apply_actions(self) -> None:
        # 每个【物理 substep】被调用
        ...
        self._entity.data.write_ctrl(torque, actuator_ids=self._actuator_ids)
```

框架侧 `src/unilab/envs/manager_based_rl_env.py:385-410` 自动接线：
```python
if not self.action_manager.requires_substep_state_feedback: return
self._backend.set_pre_step_control(self._apply_manager_control)
```
**⇒ 零框架契约变更。**

### 为什么必须逐 substep
dm10：`sim_dt=0.005`（200Hz 物理）/ `ctrl_dt=0.02`（50Hz 策略）。
τ-ω 限幅依赖**瞬时 q̇**；只在 50Hz 算会让中间 4 个 substep 共用常值 → 高速段严重失真。

---

## 五、⚠️ 已知的框架限制（会直接影响方案）

### 5.1 UniLab 读不到真力矩

实测确认 `unisim` backend **没有任何** `actuator_force` / `qfrc_*` getter；
`env._backend` 里找不到 `MjData`；`set_pre_step_control` 钩子只给 `(backend, ctrl)`。

**⇒ 力矩只能"重构"**（`τ = kp(ctrl−q) − kd·q̇` 再截断），这也是上游的做法。

**⚠️ 更关键**：换成 `<dcmotor>` 后 `gainprm/biasprm` 语义变了
（dcmotor 不符合 affine 结构），**该重构公式只在 baseline 上成立**。

**解决方案（学上游）**：让 action term 自己把算出的力矩**存下来并暴露**
（`bam_action.py` 的 `applied_torque` property，注释写着
"Closest observable to the upstream actuator_force"）。

### 5.2 sim2sim 契约拦不住执行器改动

`src/unilab/utils/sim2sim.py`：
- `ALLOWLIST` 含 `env.scene`（actuator 就在 scene XML 里）
- `DENYLIST` **无任何 actuator 字段**

**⇒ 换执行器不报错、不警告、静默通过。** 要自建校验。

---

## 六、评估方法（Q1/Q2/Q3）

**文档**：`~/dm-actuator-model/docs/评估方法_Q1Q2Q3_20260917.md`

| | 问题 | 状态 |
|---|---|---|
| **Q1** | 模型像不像真机？ | ✅ **已完成**（平均误差 0.85%）|
| **Q2** | 换模型后策略还能不能走？ | ⬜ **本次要做的** |
| Q3 | 重训后是否更好？ | ⬜ 以后 |

### ⚠️ Q2 的预期（必须提前知道）

你的历史实测（`~/reports/DM10执行器建模_诊断与方案_20260912.md`）：
**`|q̇|` 峰值 8.0~12.06 rad/s，6/6 片段全部超过 5.0 rad/s**，
而电机空载上限只有 6.4 rad/s。

**⇒ Q2 很可能得出"策略在新模型下会垮"。**
**但那是模型变准的表现，不是策略变差** —— 说明旧的 `forcerange=±28`
一直在掩盖真机做不到的行为。

---

## 七、下一步的建议顺序（2026-09-17 已全部完成 → 见 §十一）

1. ~~先读 `dm10.xml` 当前状态~~
2. ~~决定路线~~ → **B′**：原生 `<dcmotor input="voltage">` + Python 算电压
3. ~~写 action term~~ → `unilab/tasks/motion_tracking/dm10/actuator_action.py`
4. ~~跑 Q2 测量~~ → **已完成，结论见 §十一**

### 两条路线（★ 已拍板选 B′）

| | **路线 A：原生 `<dcmotor>`** | **路线 B：`<motor>` + Python 算 τ** |
|---|---|---|
| XML 改动 | 换标签 | 改 `<motor>` |
| τ-ω | MuJoCo 内部算 | 自己算 |
| 反电动势/热/LuGre | **原生支持** | 要自己写 |
| 与现有 kp/kv 冲突 | 有（语义变了） | 无 |
| 代码量 | 少 | ~450 行 |

**A 更省事**（MuJoCo 原生能力更强），但要注意 `gainprm/biasprm` 语义变化对
其他依赖它的代码（如 `joint_torque_l2`）的影响。

---

## 八、必须遵守的纪律（血泪教训）

**memory `unilab-measurement-pitfalls`（七个坑）**：
- seed=0 **且** 清空 DR（`override['events']={}`）
- **必须 32 env**（单 env 的 episode 长度只有 1/3.8）
- done 步**整帧丢弃**（reset 传送污染，滑移虚高 5.5 倍）
- 用**固定步数预算**，不用"凑够 N 个 episode"（右删失）
- **≥4 seed** 才下"显著"结论
- 报数字注明**单腿/两腿**、**池化/逐 episode 等权**

**主判据用 `Episode_Reward/*` 单步归一化值，不用 `mean_reward`。**

---

## 九、相关资产位置速查

| 用途 | 路径 |
|---|---|
| 达妙参数 + 验证工具 | `~/dm-actuator-model/` |
| 上游 action term 参考 | `~/dm-actuator-model/reference/bam_action.py` |
| 架构细读 | `~/dm-actuator-model/docs/bam_action_源码细读_20260917.md` |
| 评估方法 | `~/dm-actuator-model/docs/评估方法_Q1Q2Q3_20260917.md` |
| dm10 模型（基线，**未改动**） | `~/UniLab/src/unilab/assets/robots/dm10/dm10.xml` |
| **dm10 真实执行器变体** | `~/UniLab/src/unilab/assets/robots/dm10/dm10_actuator.xml` |
| **变体场景** | `~/UniLab/src/unilab/assets/robots/dm10/scene_flat_actuator.xml` |
| **新 action term** | `~/UniLab/src/unilab/tasks/motion_tracking/dm10/actuator_action.py` |
| **Q2 测量脚本** | `~/UniLab/_measure_q2_actuator.py` |
| **力矩重构校验** | `~/dm-actuator-model/tools/verify_dm10_torque_reconstruction.py` |
| **自检的自我测试** | `~/dm-actuator-model/tools/test_actuator_selfcheck.py` |
| 现有测量脚本（旧） | `~/UniLab/_measure_*.py` |
| 旧执行器报告 | `~/reports/DM10执行器建模_诊断与方案_20260912.md` |

### 相关 memory
- `mujoco-native-dcmotor-exists` —— MuJoCo 原生 dcmotor 能力 + 版本坑
- `unilab-actuator-model-port-precedent` —— 上游先例 + 框架限制
- `unilab-measurement-pitfalls` —— 七个测量坑
- `damiao-feedback-frame-facts` —— 达妙电机实测事实
- `pd-semantics-sim-vs-mit-differ` —— PD 语义差异

---

## 十一、⭐ Q2 已完成（2026-09-17）—— 结论与意外发现

### 11.0 一句话结论

> **策略没有被 τ-ω 打败。真正的变量是「阻尼」，不是「力矩上限」。**
> 把阻尼配平后，**dcmotor 模型下的策略反而比基线跑得更好**（178.8 vs 162.3 步，
> 终止时走完片段 98% vs 87%）。

### 11.1 完整统计：4 seed × 5 条件（`logs/q2_grid/`，20 次运行 0 失败）

主判据 = **episode 长度**（步 @50Hz，片段上限 417）。
⚠️ **本表的 `kp_v=24.5` 给出 `dτ/de = 55.1`，是基线的 1.84 倍 —— 不是"同一刚度"。**
真正的刚度等价是 `kp_v = 30·R/K = 13.333`（见 §11.7 的干净版对照）。
本表因此**混着"刚度"这个因子**，其结论（阻尼单调影响性能）仍成立，但数字不可与干净版混比。
（脚本 `_run_q2_grid.sh`，汇总 `_summarize_q2.py`）

| 条件 | kd_v | kff_v | **总阻尼** | seed0 | seed1 | seed2 | seed3 | **均值±sd** | vs baseline |
|---|---|---|---|---|---|---|---|---|---|
| **P0** baseline | kd=3.0 N·m | — | **3.00** | 170.9 | 166.6 | 146.4 | 165.3 | **162.3 ± 10.9** | — |
| **P1** kd0 | 0 | 0 | **8.44** | 150.6 | 159.0 | 161.2 | 149.3 | **155.0 ± 6.0** | ❌ 区间重叠 |
| **P2** kd083 | 0.8333 | 0 | **10.32** | 154.8 | 123.6 | 126.9 | 136.5 | **135.5 ± 14.0** | ❌ 区间重叠 |
| **P3** kdmatch | 2.4164 | 0 | **13.88** | 126.5 | 123.5 | 106.8 | 100.8 | **114.4 ± 12.5** | ✅ **p=0.029** |
| **P4** kdneg | 0 | **2.4164** | **3.00** | 175.6 | 175.5 | 164.6 | 199.5 | **178.8 ± 14.7** | ❌ 区间重叠（但更高） |

（总阻尼 = `kd_v·K/R + K²/R`；P4 用 `kff_v` 前馈抵消反电动势，见 §11.3）

终止原因分布（池化）：

| 条件 | motion_clip_end（走完片段）| anchor_pos（摔倒） |
|---|---|---|
| P0 baseline | **87%** | 13% |
| P1 kd0 | 81% | 19% |
| P2 kd083 | 68% | 32% |
| P3 kdmatch | 53% | 47% |
| **P4 kdneg** | **98%** | **2%** |

### 11.2 ⭐ 机制：阻尼是主导变量，τ-ω 力矩上限不是

**总阻尼单调增 ⇒ episode 长度单调降**（3.00→162.3, 8.44→155.0, 10.32→135.5, 13.88→114.4）。
而 τ-ω 力矩上限**全程没被碰到**：

| 条件 | \|τ\|p99 / 堵转 54 | \|q̇\|p99 / 空载 6.40 | \|v\|p99 / 电源 24 | 电压饱和率 |
|---|---|---|---|---|
| baseline | 0.177 | 0.464 | — | — |
| P1 kd0 | 0.226 | 0.358 | 0.383 | 0.011% |
| P3 kdmatch | 0.241 | 0.297 | 0.331 | 0.000% |
| P4 kdneg | 0.249 | 0.680 | 0.702 | 0.278% |

**所有比率都远小于 1** ⇒ 策略的工作点缩在物理可行域内部（图：`q2_tau_omega.png`）。

### 11.3 ⚠️ kd_v 有一个**物理下限**，这是最容易做错的地方

反电动势给关节自带 `K²/R = 8.44` N·m/(rad/s) 的阻尼，而基线 `<position kv=3>`
的总阻尼只有 **3.0**。所以：

> **`kd_v ≥ 0` 时，总阻尼永远 ≥ 8.44 —— 比基线大 2.8 倍以上，调不掉。**

要让总阻尼等于 `D`，必须加反电动势**前馈补偿**（新增的 `kff_voltage`）：

```
kff_v = (K/R) − D·R/K        （kd_v = 0 时）
D = 3.0  ⇒  kff_v = 2.25 − 1.3333 = 2.4164
```

**实测验证**（`_verify_kd_sign.py`，独立于网格）：

```
dv/dq̇ = +2.41640   预期 kff_v−kd_v = +2.41640   ✅
dτ/dq̇ = −2.99836   预期 −2.99836                ✅
基线 <position kp=30 kv=3> 的 dτ/dq̇ = −3.0      → 复现精度 0.05%
```

⚠️ **`kff_v` 是前馈不是反馈增益** —— 它不改变稳定性，只是把电机被反电动势
拿走的力矩补回来。

#### ⭐⭐ G12 已定案（2026-09-17）：固件**不做**反电动势补偿 —— 而我先前的解释是错的

**我先前写**：「`kff_v` 模拟真机固件做的反电动势补偿（达妙是否做，尚未核实）」。
**这个解释是错的。**

**实读证据（DMTool v2.1.8.4 控制框图 + 参数页，2026-09-17）**：

```
p_des ──►(+/-)──► Kp ──┐
                       ├──►(+)──► T_ref ──► [1/KT_OUT] ──► I_qref
v_des ──►(+/-)──► Kd ──┘                └──► 0 ──────────► I_dref
```
- 框里**没有任何反电动势 / Ke·ω 项**
- 固件把力矩**折算成电流**下达（`I_qref = T_ref/KT_OUT`），`I_dref = 0`
- 官方参考图与三台电机的框图**完全一致**

**⇒ 结论**：达妙固件是**电流环**，反电动势由**内环电流环的电压调节隐含处理**
（为了维持电流，电压会自动升高直到母线天花板），**不是显式前馈补偿**。
寄存器表里也没有任何反电动势项。

**那 `kff_v` 到底是什么？** 更准确的描述是：
> **把"电压域 PD"翻译成"力矩域 PD"的换算项。**

因为我们的动作环是 `v → MuJoCo dcmotor(τ = (K/R)(v − K·q̇))`，
即在**电压域**做 PD；而真机是**力矩/电流域** PD。
`kff_v = (K/R)` 恰好抵消 `−K²/R·q̇` 项，使净阻尼回到目标值。

**数学上等价，物理解释不同** ⇒ 但**这不影响 Q2/Q3 的有效性**（两臂在同一抽象下对比）。
**影响的是"Q3 配置对不对"** —— 见 §11.10 的待办。

### 11.4 ⭐ 隔离实验：把 "P4 > P0" 拆开（2026-09-17 第二段，36 次运行 0 失败）

P4 比 P0 好（178.8 vs 162.3），但差异来源不明。做了 2 段因子实验拆解。

**第一段（不干净 —— 我漏了刚度因子）**：`kp_v=24.5` 给出 `dτ/de = 55.125`，
是基线 30 的 **1.84 倍**。我先前写"kp ≡ 30 已配平"是**错的**。

| 条件 | 刚度 | 净阻尼 | kff_v | 力矩上限 | 均值±sd | 走完片段 |
|---|---|---|---|---|---|---|
| P0 baseline | 30.0 | −3.00 | 无 | 28 常数 | 162.3 ± 10.9 | 87% |
| P6 sat28_noff | 55.1 | −8.44 | 0 | 28 常数 | 148.0 ± 14.5 | 79% |
| P4 kdneg | 55.1 | −2.998 | 2.4164 | **54 τ-ω** | 178.8 ± 14.7 | 98% |
| P5 sat28_ff | 55.1 | −2.998 | 2.4164 | 28 常数 | 181.4 ± 13.1 | 98% |

**第二段（干净 —— 刚度/阻尼/力矩上限全部配平）**：`kp_v = 30·R/K = 13.3333`

| 条件 | 刚度 | 净阻尼 | kff_v | 力矩上限 | 均值±sd | 走完片段 |
|---|---|---|---|---|---|---|
| **P0** baseline | 30.0 | −3.00 | 无 | 28 | **162.3 ± 10.9** | 87% |
| **P9** match_noff | 30.0 | −3.00 | 0 | 28 | **89.0 ± 8.2** | **36%** |
| **P10** match_ff | 30.0 | −3.00 | 2.4164 | 28 | **161.7 ± 12.1** | 86% |

```
干净版（P0/P9/P10 三项物理量全同，只差实现）:
  实现差异  P9  − P0  = −73.3 步   ✅ p=0.029
  前馈项    P10 − P9  = +72.8 步   ✅ p=0.029
  合计      P10 − P0  =  −0.6 步   ← 几乎完全抵消
力矩上限  P4 − P5     =  −2.6 步   ❌ 不显著
```

### 11.5 三条结论

1. **力矩上限无关** —— 把上限从 54 压到 28（常数），差异 −2.6 步且区间重叠。
   工作点根本没碰到上限（`|τ|p99/堵转 ≤ 0.25`）。
2. **前馈项 `kff_v` 是必需的，不是可选优化** —— 没有它阻尼变成 −8.44（基线的 2.8 倍），
   策略崩到 89 步、**只有 36% 能走完片段**。加上它 +72.8 步。
3. **实现差异真实存在（−73 步）但被前馈完全补回**。全配平下 P10 (161.7) ≈ P0 (162.3)。

**⇒ 对 Q3 的直接含义**：`kff_v` **不能省**。要么保留，要么重训让策略适应 8.44 的阻尼。

### 11.6 ⚠️ 我在这一轮犯的两个错（都记下来防重犯）

1. **换算乘除搞反**：`dτ/de = (K/R)·kp_v`，所以要 `dτ/de=30` 需 `kp_v = 30·R/K = 13.333`。
   我先后用过 `1.6667`（得 3.75，只有基线 1/8 → 假崩溃）和 `24.5`（得 55.125，1.84 倍）。
   **自检法**：算完回头验 `kp_v·K/R` 是否等于目标。本机 `K/R=2.25`、`R/K=0.4444`。
   代码里已写进 `actuator_action.py` 的 docstring。
2. **声称"已配平"而没验**：表格里写"kp ≡ 30 同一物理刚度"，实际是 55.1。
   **凡是"已配平"的声明，都要像 τ-ω 那样实测斜率验证**（`_verify_kd_sign.py` 的做法）。
3. **设计了一个没有判别力的条件**：给 P6 加 `saturation=89` 想分离"限幅域"，
   但 89 > 54 堵转 ⇒ 根本不生效。**设计对照前先问「这个约束会不会 binding」**。
4. **`pkill` 时误删了自己的探针脚本**（写成 `/tmp` 又被自己清掉）—— 探针脚本放仓库里。

### 11.7 ⭐⭐ 根因已定位（2026-09-17 第三段）—— **是 kff_v，不是"未解之谜"**

**先说结论**：P9 − P0 = −74 步，根因就是 **`kff_v`（反电动势前馈项）本身**。
`kff_v = 0` ⇒ 净阻尼 = `−K²/R = −8.435`；`kff_v = 2.4164` ⇒ 净阻尼 = `−2.998` ≈ 基线。
**没有更深的原因。** Q2 的结论本来就对。

#### 决定性复现（同一探针、同一进程、各 4 seed，600 步）

```
         seed0    seed1    seed2    seed3     均值 ± sd        range
P0       171.9    147.4    167.6    166.3    163.3 ± 10.9   [147.4, 171.9]
P9       96.9     78.4     89.4     92.5     89.3 ±  7.9    [ 78.4,  96.9]   ← kff_v=0
P10      175.0    146.0    174.9    154.8    162.7 ± 12.1   [146.0, 174.9]   ← kff_v=2.4164

P9  vs P0:  −74.0 步  ✅ 区间分离 p=0.029
P10 vs P0:   −0.6 步  ❌ 区间重叠（不显著）
P0 的种子噪声跨度 = 24.5 步
```
三个条件**全部复现了记录值**（89.0 / 161.7 / 162.3）⇒ 记录无误。

#### ⚠️ 那我这一轮在追什么？—— 一个我自己制造的幻影

**并排探针里我写的是 `kff=2.4164`，即 P10 的配置，却一直拿结果与 P9（89 步）比。**
于是凭空产生了一个"矛盾"：

> ❌ "同状态力逐位相同（≤5e-15），轨迹却分歧 0.45 rad/s —— 必有未捕获的差异！"

**这个矛盾不存在。** 探针测的是 P10，而 **P10 ≈ P0**（差 −0.6 步，区间重叠）。
两臂本来就该等价 ⇒ 那 0.45 rad/s 是**混沌噪声**，不是机制。

#### 因此以下"排除项"要重新定性

之前列的 6 项排除（重构误差 / 写入路径 / 0.05% 残差 / 截断域 / 隐式积分 / 映射错位）
**结论仍然成立**（那些实验本身做对了），但它们**排除的是一个不存在的问题**。
其中两条有独立价值：

1. **力矩重构与 MuJoCo `actuator_force` 逐位相同（≤1e-15）** —— 这是真结论，
   Q2 的力矩通道可信。
2. **增益敏感性：±0.05% 扰动时 4 seed 逐位相同；±0.5% 只值 1 步** —— 真结论，
   说明基线对增益**不敏感**（与"P9 因阻尼差 2.8 倍而崩"不矛盾：那是 2.8 倍，不是 0.05%）。

#### ⚠️ 真正暴露的问题：我的探针纪律有漏洞

配置项一多（kp/kd/kff/saturation/模型文件），**手写探针极易把条件写错**。
本轮已有 `--out-tag` 机制，但**手写探针绕过了它**。

---

#### ⭐⭐ 2026-09-17 审计：上一条"增益敏感性"的**证据链是坏的**（已修并重跑）

上一条写的「**±0.05% 扰动时 4 seed 逐位相同；±0.5% 只值 1 步**」—— **不成立**。审查发现：

**`_measure_q2_actuator.py` 的 `--model-file` 只接在 `if USE_DCMOTOR:` 分支里**，
而 `_run_q2_sensitivity.sh` 传的是 `--actuator baseline`
⇒ **override 被静默丢弃**，16 次"敏感性"运行**逐位等于基线**
（npz 的 `model_file` 字段暴露了：写的是 `scene_flat.xml` 而非扰动变体）。
那些扰动 XML **从未被加载过**。

**修复**：给测量脚本加**统一参数自检** —— 每个影响结论的参数
（模型文件 / 执行器类型 / saturation / 三个增益 / seed / DR 数量）都从**已构建的 env**
读回**实际生效值**，与意图逐项比对，不符即 `raise SystemExit`：

```
✓ 变体已加载：kp=30.150000 kv=3.015000 forcerange=[-28. 28.]
✓ 参数自检通过：actuator=baseline  model=scene_sens_p1.xml  seed=0  DR=0 个
```

**重跑结果（16/16，全部通过自检）**：

| δ | seed0 | seed1 | seed2 | seed3 | 均值±sd | vs δ=0 |
|---|---|---|---|---|---|---|
| 0 | 170.9 | 166.6 | 146.4 | 165.3 | **162.3 ± 10.9** | — |
| +0.05% | 171.9 | 169.3 | 145.6 | 165.3 | 163.0 ± 11.9 | +0.7 |
| −0.05% | 170.6 | 155.9 | 160.4 | 172.3 | 164.8 ± 7.9 | +2.5 |
| +0.5% | 154.7 | 170.4 | 170.1 | 156.1 | 162.8 ± 8.6 | +0.5 |
| −0.5% | 163.9 | 175.0 | 166.8 | 159.0 | 166.2 ± 6.7 | +3.9 |

**读法**：效应全部落在种子噪声内（δ=0 自身 sd 就是 ±10.9），且**无单调趋势**
（+0.5% 是 +0.5 步、−0.5% 是 +3.9 步，方向都不一致）⇒ **噪声主导**。

⚠️ **我一度据 seed0 单点写"+0.5% 掉 16 步"** —— 4 seed 后抹平。**又一次"先看单点后看全体"**（见 pitfall 7）。

**⇒ 结论方向不变、证据链重建**：即使 ±0.5%（**10 倍于残差**）也只值 +0.5~3.9 步，
而 73 步是它的 **19~150 倍** ⇒ 0.05% 的增益残差解释不了它。
⚠️ 但旧证据是"什么都没发生的 0"，新证据才是"测过之后确认是 0" ——
**前者无法排除"更大扰动会有大效应"，后者可以。**

#### 同批审计的其余 4 项（参数确认真生效）

| 候选 | 参数生效性 | 结论 |
|---|---|---|
| 力矩重构精度 | ✅ 独立脚本 `_verify_baseline_torque.py` | ❌ ~1e-15 |
| **截断域（电压 vs 力矩）** | ✅ **已补验**：两补丁命令电压差**均值 2.0 V、61% 样本 >1 V** | ❌ 结果仍逐位相同（92.2）|
| 隐式积分 | ✅ 读 XML + 改 XML 重跑 | ❌ 环境用 Euler |
| 执行器→关节映射 | ✅ 打印 `target_ids` | ❌ 两臂都是 `[0..9]` |

**截断域那项的补验值得单独说**：原来只能说"两次跑结果一样"，
现在能说"**我把饱和时的阻尼项改掉了（电压差均值 2 V、61% 样本 >1 V），性能没有任何变化**"
⇒ 证据力从"平凡"升级为"确证"。**这是审计的正确做法：先证明补丁真的改了行为。**

**为什么这不止是修一个 bug**：同族"参数没生效却静默通过"本会话出现 **4 次**
（`--out-tag` 缺失 / 变量搞混 / `saturation=89` 不 binding / `--model-file` 被丢弃）
⇒ **是管线缺一道强制检查，不是偶发。**
**教训：探针必须复用测量脚本的 override 构造，或至少输出里回显全部生效参数。**

### 11.8 ⚠️ 本轮我犯的错（按严重度）

1. ⭐⭐ **变量搞混，凭空造出一个"未解之谜"追了一整轮** —— 探针写 `kff=2.4164`（P10）
   却与 P9 的记录值比。**一条对的数值（162.7 应对 161.7 不是 89.0）本可立刻暴露。**
   **新纪律：探针跑完先核对"这个数应对应哪条记录"。**
2. ⭐ **读数方法本身没被验证** —— 三次读 `term.target` 得到三个答案。
   **在 action term 内部采样才是可信读数点。**
3. **换算乘除搞反**：`dτ/de = (K/R)·kp_v` ⇒ 要 30 需 `kp_v = 30·R/K = 13.333`
   （我用过 1.6667 得 3.75、24.5 得 55.1）。同类错误本会话犯两次。
4. **声称"已配平"却没验** —— 表格写"kp ≡ 30"，实际 55.1。
5. **设计了没有判别力的条件** —— `saturation=89 > 54 堵转`，不 binding。
6. **差点在已是 Euler 的环境做"换成 Euler"的实验**（同义反复）。
7. **两次 `pkill` 误删自己的脚本。**

### 11.9 实操注意（累积）

```bash
bash _run_q2_grid.sh            # 20 次（主网格 + 隔离）
bash _run_q2_matched.sh         # 8 次（配平对照 P9/P10）
bash _run_q2_sensitivity.sh     # 16 次（增益敏感性）
uv run --no-sync python _analyze_q2_isolation.py logs/q2_grid walk_left90
uv run --no-sync python _analyze_q2_sensitivity.py logs/q2_grid walk_left90
uv run --no-sync python _probe_substep_torque.py     # 逐物理子步力矩对比
uv run --no-sync python _verify_baseline_torque.py   # 证明基线重构逐位可信
uv run --no-sync python _plot_q2_tau_omega.py logs/q2_grid walk_left90 q2_tau_omega.png
```

- ⚠️ **`--out-tag` 必须显式给**（文件名不含增益 ⇒ 条件互相覆盖，白跑 5 次）
- ⚠️ **`kp_v = 30·R/K = 13.333` 才是刚度等价**（不是 1.6667，也不是 24.5）
- ⚠️ **`saturation=N` 只对 N < 54 堵转才有约束力**
- ⚠️ **XML 注释里不能出现 `--`**
- ⚠️ **执行器→关节映射用 `actuator_trnid`**（名字不匹配）
- ⚠️ **`f"{nan:>8.1f}"` 只有 3 字符宽** ⇒ 表格错位，用 `f"{'—':>8}"`
- ⚠️ **matplotlib 中文字体**只有 `Droid Sans Fallback`
- ⚠️ **`make check` 会全仓库重排格式**（实测改 17 个无关文件）→ 定向 `ruff check <file>`
- ⚠️ **改环境默认值前先确认默认值**（`dm10.xml` 没写 `integrator` ⇒ 就是 Euler）

### 11.9b ⭐⭐ G11 已定案（2026-09-17）：固件 TMAX = 28 N·m

**起因**：联网检索发现达妙是**电流模式**固件（MIT: `T_ref = Kp·e + Kd·ė + τ_ff`，
然后 `i_q,ref = T_ref/K_T`），寄存器表**没有反电动势补偿项**。怀疑我们的模型
（只有电压上限、堵转 54 N·m）高估了真机能力。

**答案在我们自己的实测记录里** —— `robot-deploy-toolkit/docs/01-通讯打通/0-index.md:231`：

```
【实测】本机 4 台 DM-J4340-2EC 24V 实读：
  PMAX = 12.5    VMAX = 10.0    TMAX = 28.0     ← 0x17 寄存器
协议：t = range_map(f_t, -TauMax, +TauMax, 0, 4095)   ← TMAX 就是力矩指令满量程
```

| | 值 | 说明 |
|---|---|---|
| 台架曲线外推的**物理**堵转 | 54 N·m | 真实但**不可达**（固件截住）|
| **固件 TMAX**（力矩指令上限）| **28 N·m** | 真机能达到的实际上限 |
| **基线 `<position forcerange=±28>`** | **28** | ✅ **无意中是对的** |
| 我们的 `<dcmotor>`（删了 forcerange 默认块）| **54** | ❌ **高 1.93 倍** |

**⇒ 结论**：τ-ω 形状（我们加的）是真的，但**我们模型的可行域在低速段比真机高 1.93 倍**。

**为什么之前没暴露**：Q2 实测 `|τ|p99 = 13.4 N·m`、工作点全在 28 以下 ⇒
**两个上限都不 binding**。所以这次修正**不会改变已得结论**，但**对未来更快/更用力的策略会 binding**。

**修法（一行）**：给 `dm10_actuator.xml` 的 10 个 dcmotor 都加 `saturation="28 0 0"`。
（※ 我们已有的 `saturation=28` 变体**就是**这个 —— 等于说 P5/P6/P9/P10 那批**比 P4 更接近真机**。）

#### ⚠️⚠️ 顺带发现的【部署侧静默 bug】——比仿真侧 gap 更严重

`roboparty_deploy` 代码里写的是 `{12.5, 6, 12, 500, 5}`，**TauMax = 12**，而电机里是 **28**：

| 方向 | 后果 |
|---|---|
| **下行**（发指令）| 你要 12 N·m → 电机解成满量程 → **实际给 28 N·m（放大 2.33×）** |
| **上行**（读反馈）| 电机实际 28 N·m → 代码解成 **12 N·m（低估 2.33×）** |

⇒ **命令被放大、读数被缩小。你会以为"才用了 12 N·m"，实际已打到满量程。**
⇒ **这是真机上会直接导致失力/过流的问题，优先级高于仿真侧的任何 gap。**
（原文档已标"必须在第一次上电前定案"，但**需确认代码是否已改** —— 用 grep 验，别信记忆。）

#### 另一个待澄清项：VMAX = 10.0

实测 VMAX = **10.0 rad/s**，但台架曲线 τ=0 外推到 **6.4017**、标称隐含 **5.49**。
⇒ 三个数不一致。VMAX 是固件限幅，不必等于物理空载转速，但**差异需要澄清**
（可能影响高速段）。**建议用 DMTool 复核一次，并做一次自由摆动测真实空载转速。**

### 11.10 还没做的

- **全片段矩阵**（11 片段 × 4 seed）未做 —— 现在只有 `walk_left90`
- 长跑（steps 3000）、DR 打开、多策略 —— **均未做**
- ~~`kff_v` 的真机依据未核实~~ → **✅ 已定案（§11.3）：固件不做补偿**

---

## 十二、开新会话时的第一句话建议

> ⭐ ⭐ **（2026-09-17 最新）直接读这句就够：**
>
> ```
> 读 ~/UniLab/HANDOFF-dm10-actuator.md。Q1/Q2/Q3 都做完了（§11–§14），
> 现在修上机阻塞：G1（观测可得性，先跑仿真消融）+ obs 顺序的根治（防呆机制）。
> ```

**当前状态一句话**：执行器层（Q1/Q2/Q3）已收尾，**Q3 结论是"权衡"**（更省力更慢，
但总奖励略降 —— 详见 §14.6）。**剩下的是上机阻塞，不是仿真问题。**

### 上机路线（按优先级）

| # | 待办 | 状态 | 备注 |
|---|---|---|---|
| **G1** | **观测可得性**：`base_lin_vel` 等真机测不到 | ⬜ **下一个要做的** | 先跑仿真消融（不可得维置零，量损失）→ 再决定扩框架还是重训。清单见 memory `dm10-65dim-undeployable-obs-gap` |
| **G2** | 观测顺序 | ✅ **已修**（2026-09-17） | `roboparty_deploy/.../dm10/configs/default.yaml:5` 改了一行 + 加详细注释 |
| **G2b** | **obs 顺序的根治（防呆）** | ⬜ **建议做** | 现在只改了这一行。根治 = 导出 onnx 时 dump「obs 语义清单」，部署侧比对（`run_config.json` 的 `contract_snapshot['env.observations']` 已逐字保存 term 顺序，读它即可）|
| **G3** | 12 个连杆质量是"猜的不是称的" | ⬜ 硬件 | 登记表实测列全空。**最便宜的收益确定性最高的系统辨识** |
| G5 | 速度低通滤波是否存在 | ⬜ 未测 | 单关节辨识时顺带看 q̇ 相位滞后 |

### 🔍 G1 逐维判定表（2026-09-17 实测核对，**推翻 memory 的"缺 12 维"**）

Q3/基线策略的 `policy.onnx` 输入 = **65 维**。逐项核对了「部署框架能否提供」：

| 训练侧 term | 维度 | 需要什么 | 真机可得 | 难度 |
|---|---|---|---|---|
| `command` | 20 | 参考轨迹（**查表**）| ✅ 纯查表 | **零** |
| `motion_anchor_ori_b` | 6 | 机器人**世界姿态**（IMU 四元数）| ✅ IMU 有 | **零** |
| `base_ang_vel` | 3 | IMU 陀螺 | ✅ | **零** |
| `joint_pos` | 10 | 编码器 | ✅ | **零** |
| `joint_vel` | 10 | 编码器差分 | ✅ | **零** |
| `actions` | 10 | 上一步动作 | ✅ | **零** |
| **`motion_anchor_pos_b`** | **3** | 机器人**世界位置** + IMU 姿态 | ⚠️ **位置需状态估计** | **中** |
| **`base_lin_vel`** | **3** | 世界线速度 | ❌ **无源** | **高** |
| **合计** | **65** | | | |

**⭐ 结论：真正"什么都没有"的只有 `base_lin_vel` 3 维，不是 12 维。**

**依据（读了 kernel 源码）** —— `motion_anchor_pos_b` 的计算是：
```python
anchor_v = motion_anchor_pos_w − robot_anchor_pos_w        # 需机器人世界位置
anchor_pos_b = R(robot_anchor_quat_w)⁻¹ · anchor_v          # 需机器人世界姿态
```
⇒ 姿态有 IMU ✅；**位置才是问题** —— 但这不是"传感器没有"，而是**"odometry/状态估计"**问题：
真机不知道自己在世界里的位置 ⇒ 只能假设从原点出发 ⇒ 误差会**累积漂移**。

**⇒ `motion_anchor_pos_b` 是「中」难度（可用"从原点积分"近似），`base_lin_vel` 才是「高」。**

**⚠️ 注意部署侧已有 `motion_command` 观测源**（`obs_manager.cpp:78`）——
但读源码发现它**只是回放参考轨迹**（`motion_loader->get_pos/get_vel`），
**不含任何机器人状态** ⇒ 对应训练的 `command`(20维)，不是锚点误差族。

### G1 该怎么走（**消融要分开做，别一起置零**）

| 步骤 | 做法 | 决定什么 |
|---|---|---|
| **C1** | 只把 `base_lin_vel` 置 0，量退化 | **决定 A 还是 B** |
| **C2** | 再把 `motion_anchor_pos_b` 置 0，量退化 | 决定要不要为它做状态估计 |

判据（沿用 memory）：**退化 <5% ⇒ 走 A（扩框架，不用重训）；>20% ⇒ 必须走 B（重训 56 维）。**

工具：`_measure_q2_actuator.py`（已支持各种 override，2026-09-17 加了 `start_frame`/`reward`）
+ `_analyze_q3_stratified.py`（五层报告）。
⚠️ **A 路（扩框架）要改 C++ `obs_manager.cpp` 并重新编译上板** —— 这是代价所在，不是仿真侧。

### ⚠️ 上机前必须复核的两条

1. **训练只训过"直走"**（`lin_vel_x=[0.3,0.5]`，`lin_vel_y=0`，`ang_vel_z=0`），
   而部署 `clip_cmd` 给了 ±0.4/±0.8 余量 ⇒ **侧移/转向是分布外** ⇒ 上机先只发 vx
2. **部署代码的力矩量程已改**（`tau_max` 12→28、`vel_max` 6→10，5 处）
   ⇒ **必须重新整定 `kp`/`kd`** —— 原先的增益很可能是在 2.33× 力矩下调出来的

### 历史（已完成，保留备查）

~~下一步 **Q3：用 `dm10_actuator.xml` + `DmVoltageActionCfg` 重训**~~
~~- 直接理由：**阻尼失配**（真机 `K²/R = 8.44` vs 训练用的 3.0），不是力矩上限（已排除）~~
~~- **`kff_v` 决定一切**：`kff_v=0` ⇒ 89 步（36% 走得完）；`kff_v=2.4164` ⇒ 162.7 步（≈基线）~~
~~- 训练配置建议：**总阻尼 D 对应 `kff_v = kd_v + (K/R) − D·R/K`**（本机 `K/R=2.25`、`R/K=0.4444`）~~

**Q3 已完成**（§14.6）：训练 2 seed × 25000 iter，2×2 评估 × 4 seed。
结论 = **权衡**：`|τ|` −5.8%、`|q̇|` −9.9%（都 p=0.002，更省力更慢），
但总奖励 −1.5%、尾部坏帧 +1.16pp。

---

## 十三、⚠️ 会话级教训（写给下一个会话的我）

**本轮最大的坑：变量搞混会凭空造出一个"未解之谜"。**

并排探针里写了 `kff=2.4164`（P10），却拿结果与 P9（89 步）比 ⇒
凭空造出"力相同却轨迹分歧"的假矛盾，追了一整轮（8 个实验 + 2 个 agent）。

**三条新纪律（已写进 memory `variable-confusion-fabricated-mystery`）：**

1. **探针跑完先核对："这个数应对应哪条记录？"**
   一条对的数值（162.7 应配 161.7）本可立刻暴露问题。
2. **"力相同却轨迹分歧"这类矛盾，先怀疑自己的条件搞错了，再怀疑框架。**
3. **便宜的复现优先** —— 先花几分钟把三条件并排各跑 4 seed 对记录，
   **复现不了就先查配置，不是先查物理。**

另外两条（memory `measurement-readpoint-verification`）：
- **在 action term 内部 `apply_actions` 调用点采样**才是可信读数点（事后反推会读到别的时刻）
- **凡"已配平/等价"的声明都要实测验证**（本会话同一类错误犯两次）

---

## 十四、✅ Q3 已完成（2026-09-17）—— 结论是「权衡」，且**比旧策略更适合上机**

### 14.1 训练配置

**新 task 配置**：`src/unilab/conf/flashsac/task/dm10_actuator_motion_tracking/base.yaml`
（继承 `dm10_motion_tracking/base`，**只改执行器模型 + action term**，其余全继承）

```yaml
env.scene.model_file: .../scene_flat_actuator.xml
env.actions.joint_pos._target_: ...DmVoltageActionCfg
  scale: 0.25   use_default_offset: true   command_name: motion
  kp_voltage: 13.3333333   # = 30·R/K  ⇒ dτ/de = 30 = 基线
  kd_voltage: 0.0
  kff_voltage: 2.4164      # 前馈 ⇒ 净阻尼 −2.998 ≈ 基线
```

**⚠️ 为什么必须写进 task 配置而不是用运行期 override**：
`run_config.json` 会把 `env.actions` 记进 `contract_snapshot`，而 `env.actions` 在
sim2sim 的 **DENYLIST** 里 ⇒ 用 override 训出来的 checkpoint 在 play 时会被**拒绝**。
已用冒烟测试确认 snapshot 正确记录（`_verify_q3_config.py`）。

**与基线逐项对齐**（8 个偏离默认的参数全部显式覆盖，见 `_run_q3_train.sh`）：
`num_envs=2048, max_iterations=25000, batch_size=2048, updates_per_step=4,
gamma=0.99, tau=0.05, num_atoms=501, save_interval=2500, use_compile=false`

⇒ **与基线只差「执行器模型」一个因子。**

### 14.2 评估设计：2×2 矩阵（不只是"新旧策略比拼"）

```
              │  旧模型 <position ±28>   │  真实执行器 dcmotor
  ────────────┼──────────────────────────┼────────────────────────
  旧策略      │  Q2: baseline = 162.3    │  Q2: match_ff = 162.7
  新策略      │  Q3: new*_on_pos         │  Q3: new*_on_dc
  ────────────┴──────────────────────────┴────────────────────────
```

**为什么 4 个格子都要测**：只测「都在 dcmotor 上」会漏掉一个关键问题 ——
**新策略是"变强了"还是"过拟合了新模型"？**
（在 dcmotor 上更强、但在旧模型上更弱的策略 = 更脆）

三个判据：
1. `new_on_dc − old_on_dc` 显著为正 ⇒ 重训有效
2. `new_on_pos − old_on_pos` 显著为负 ⇒ ⚠️ 过拟合
3. `old_on_dc − old_on_pos` ⇒ 旧策略是否真被模型限制（Q2 已知：不显著）

**⚠️ Q2 的 `baseline`/`match_ff` 两组可直接复用**（同 clip、同 32 env、同 600 步预算、
同 seed）⇒ 只需跑新策略的两个格子。

### 14.3 脚本

```bash
bash _run_q3_train.sh     # 训练（2 seed，各 ~60min，RTX 4060 Laptop）
bash _run_q3_eval.sh      # 评估（4 格 × 4 seed）
uv run --no-sync python _analyze_q3.py logs/q2_grid walk_left90 logs/q3_grid
uv run --no-sync python _verify_q3_config.py    # 训练前的配置自检
```

### 14.4 已确认 / 待确认

- ✅ 配置合成正确（奖励/观测/sim_dt 与基线一致；action term 是 `DmVoltageAction`）
- ✅ run_config snapshot 记录了 actuator 配置 ⇒ play 不会被 sim2sim 拒
- ✅ 冒烟训练 2 迭代无错
- ✅ 训练健康（237 迭代时 `action_rate_l2 = −0.33` 非零，动作确实在起作用）
- ⬜ 训练完成（2 seed）
- ⬜ 2×2 评估

### 14.5 ⚠️ Q3 的已知局限（提前记下）

- **训练只 2 个 seed**（训练是随机过程；要下"显著"结论需更多）
- **只在 `walk_left90` 一个片段评估** ⇒ 换片段结论可能不同
- ~~`kff_v = 2.4164` 的真机依据未核实~~ → **✅ 已定案（§11.3）**：固件是电流环、
  **不做显式反电动势前馈**（控制框图里没有该项）。
  ⚠️ 但这**不**说明 Q3 的 `kff_v=2.4164` 是错的 —— 它的作用是
  「把电压域 PD 翻译成力矩域 PD」，与固件行为无关。
  **真正待办**：确认真机的 PD 到底跑在哪一域（固件 MIT 是力矩域；
  但我们 Sim2Real 时命令的是 `p_des/v_des`，PD **也在固件里**）。
  ⇒ 详见 `~/reports/` 的固件实读记录（待建）

---


### 14.6 ⭐ Q3 结论（2026-09-17 完成）

**训练**：2 seed × 25000 iter（~57 min/seed）。**评估**：4 组 × 4 seed，dcmotor+sat28，
与 Q2 的 `match_ff` 同条件（`logs/q3_grid2/`）。

#### ⚠️ 先说两个被推翻的判断（都记下来防重犯）

**① 「再加训练 seed」无用 —— 但理由不是我以为的那个**
- 实测 `between-seed var / within-seed SEM² = 0.74~0.98` ⇒ **检测不到策略层面的种子效应**
- 所谓"seed 噪声"其实是**"抽到哪些起始帧"**（采样 uniform + 固定片段 ⇒ 长度 ≈ `417 − f0`）
- ⭐ 且 `run_summary.mean_episode_length` 那个 **43.7 步的种子差是假的** ——
  它是"最后一次迭代"的**单点快照噪声**；平滑后（最后 2500 iter 均值）**只差 1.6 步** ⇒ 训练已收敛
- `logs/` 里 60+ 个历史 run **全是 seed=1**，唯一的同条件不同种子组就是 q3 的这 2 个

**② 「主指标 = episode 长度」是错的**（SNR 1.23，p=0.085）。真正强的是 `|τ| 均值`（SNR 7.98，p=0.002）。

#### 分层结果（`_analyze_q3_stratified.py`）

| 层 | 指标 | 旧 | 新 | 差 | p |
|---|---|---|---|---|---|
| **A 生存** | episode 长度 | 161.7 | 176.4 | +14.70 | 0.085 ❌ |
| | 走完片段占比 | 86.4% | 88.8% | +2.4% | 0.143 ❌ |
| **B 执行器** | **`\|τ\| 均值`** | 2.6228 | 2.4709 | **−5.8%** | **0.002** ✅ |
| | **`\|q̇\| 均值`** | 0.7196 | 0.6481 | **−9.9%** | **0.002** ✅ |
| | `\|q̇\| p95` / `\|τ\| p95` | — | — | −4.5% / −3.0% | 0.004 / 0.012 ✅ |
| **C 安全** | **超力矩时间占比** | **0.0000%** | **0.0000%** | — | 1.000 ✅ |
| | 超速时间占比 | 0.047% | 0.040% | −0.007pp | 0.087 |
| | `\|τ\|` 峰值 | 24.88 | 22.75 | −2.12 | 0.048 ✅（**都 < 固件 28**）|
| **D 跟踪** | 关节角误差 均值 | 0.2213 | 0.2228 | +0.7% | 0.671 |
| | ~~关节角误差 p95~~ | 0.6491 | 0.6706 | +3.3% | ~~0.004~~ ⚠️ **假信号，见下** |
| **E 奖励** | 总奖励（单步归一化）| 4.5317 | 4.4649 | −1.5% | 0.002 ⚠️ |

#### ⚠️⚠️ 「p95 变差 p=0.004」是**假信号** —— 聚合层级错了

| 算法 | 结果 |
|---|---|
| **池化所有帧后取 p95** | +3.3%，**p=0.004** ⇒ "显著变差" |
| **逐 episode 取 p95，再比分布** | 分位 10/25/50/75/90/95/99 = +0.7 / **−0.6** / +1.2 / +3.0 / **−1.4** / **−2.2** / +0.9 % ⇒ **噪声** |

池化把两个策略**不同的 episode 组成**（362 vs 662 个、长度分布不同）搅进了分位数。
**⇒ 尾部指标要"逐 episode 先算、再比分布"，并并列多个分位**才能分辨"普遍上移"vs"尾部事件"。
（详见 memory `aggregation-level-flips-conclusion`）

#### ✅ 「总奖励下降」是**更保守**，不是**做差了**

按占比分解（关键）：

| 奖励项 | 占旧总值 | 差 | |
|---|---|---|---|
| **`motion_body_pos`** | **42.4%** | **+0.1%** | ✅ **最大项略好** |
| `motion_global_root_ori` | 9.1% | **+2.8%** | ✅ |
| `motion_body_lin_vel` | 15.7% | −4.4% | ⚠️ |
| `motion_body_ori` / `ang_vel` / `global_root_pos` | 13/17/5% | −4/−1/−9% | ⚠️ |
| `action_rate_l2`（惩罚）| −1.3% | **惩罚更小** | ✅ |

**⇒ 占 42% 的主项变好、惩罚更小、掉的是速度/朝向项**
⇒ **"用更少力、更慢地走"，不是"任务做差了"**。真退步的话主项会一起掉。

#### 最终结论

> **新策略 = 更保守：`|τ|` −5.8%、`|q̇|` −9.9%（都 p=0.002）、位置跟踪略好、走完率略升。**
> **代价**：速度/朝向跟踪略降、总奖励 −1.5%、尾部坏帧 5.00%→6.16%。
> **不是全面改进，是一个权衡 —— 而且这个权衡的方向对真机有利**（真机怕过大力矩/过快）。

#### 上机就绪判定

| 项 | 结论 |
|---|---|
| **力矩安全** | ✅ **从不超固件 TMAX=28**（峰值 22.75，余量 19%）|
| 速度 | ✅ 峰值 7.47，超空载 6.40 仅占 0.04% 时间 |
| 力矩/速度水平 | ✅ 显著更低（p=0.002）|
| 摔倒 | ✅ 走完率 88.8% > 旧 86.4% |
| 位置跟踪（奖励 42% 主项）| ✅ 略好 |
| 尾部 | 🟡 坏帧 +1.16pp（上机观察）|

**⇒ 可以上机，且比旧策略更适合上机。但必须先修 G1/G2（观测可得性/顺序）—— 那才是真阻塞。**

#### 本次新增的资产

- `_measure_q2_actuator.py`：新增 `start_frame`（每 episode 起始帧）+ `reward`（7 项逐帧）+ `reward_names`
  ⭐ `start_frame` 让"长度比较"可做**起始帧分层的生存比较**（消除主导方差）
- `_run_q3_recollect.sh`：补采集驱动
- `_analyze_q3_stratified.py`：**五层分层报告**（生存/执行器/安全/跟踪/奖励）
- `logs/q3_grid2/`：24 npz（6 组 × 4 seed，含新字段）；`logs/q3_grid_v1/`：旧版备份
- memory `aggregation-level-flips-conclusion`
